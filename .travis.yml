language: python
python:
  - "3.6.0"
install:
  - pip install -q -r requirements.txt
services:
  - postgresql
before_script:
  - psql -c 'create database test_db;' -U postgres createuser tweetmeadmin
  - python manage.py migrate
script:
  - coverage run --source='tweetme' manage.py test
before_install:
  - pip install coveralls
after_success:
  - coveralls